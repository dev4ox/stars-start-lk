----- УДАЛЕНИЕ ТАБЛИЦ ИЗ БД postgreSQL -----
DO $$ DECLARE
    r RECORD;
BEGIN
    -- Отключение проверок внешних ключей
    EXECUTE 'SET session_replication_role = replica;';

    -- Перебор всех таблиц в текущей схеме
    FOR r IN (SELECT tablename FROM pg_tables WHERE schemaname = 'public') LOOP
        EXECUTE 'DROP TABLE IF EXISTS ' || quote_ident(r.tablename) || ' CASCADE;';
    END LOOP;

    -- Включение проверок внешних ключей
    EXECUTE 'SET session_replication_role = DEFAULT;';
END $$;



----- Экспорт - импорт таблиц через SQL (cmd) -----
pg_dump --data-only --table=panels_groupservices --column-inserts --dbname=stars_start > /home/ten/groupservices_data.sql
pg_dump --data-only --table=registration_services --column-inserts --dbname=stars_start > /home/ten/services_data.sql
pg_dump --data-only --table=registration_category --column-inserts --dbname=stars_start > /home/ten/category_data.sql
pg_dump --data-only --table=registration_customuser --column-inserts --dbname=stars_start > /home/ten/customuser_data.sql

psql --dbname=stars_start < /tmp/groupservices_data.sql

